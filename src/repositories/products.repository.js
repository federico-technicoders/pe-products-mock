import { ClassRepository } from "./class.repository.js"

export class ProductRepository extends ClassRepository {
    constructor(dao) {
        super(dao)
    }

    // Add your custom methods here
}